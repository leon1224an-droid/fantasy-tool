[phases.setup]
nixPkgs = ["python311", "gcc", "libffi"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "alembic upgrade head || echo 'Migration warning (may already be applied)'; uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
